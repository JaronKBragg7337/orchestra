# Synthesis artifacts

Each cycle will eventually contain two independent syntheses over the same sealed raw corpus:

- `synthesis-a.md` — produced by Claude Code in a context-clean directory.
- `synthesis-b.md` — produced by the harness while Synthesis A is out of scope.

Both use the item format defined in [SPEC.md](../SPEC.md). No synthesis artifacts have been produced yet.
